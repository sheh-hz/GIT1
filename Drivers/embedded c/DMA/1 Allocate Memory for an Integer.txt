#include <stdio.h>
#include<stdlib.h>

int main()
{
    int *p=NULL;
    p=malloc(sizeof(int));
    if(p==NULL)
    {
        printf("memory not allocated");
        exit(1);
    }
    *p=20;
    printf("%d\n",*p);
    printf("%d %d\n",p,*p);
    free(p);
    printf("%d %d",p,*p);

    return 0;
}