#include <stdio.h>
void display(int *arr, int n)
{
    for(int i=0;i<n;i++)
    {
        
        printf("%d\t",*(arr+i));
    }
}
int main()
{
    int arr[] = {10, 20, 30, 40, 50};
    int *p=arr;
    int len=sizeof(arr)/sizeof(arr[0]);
    display(arr,len);
    
    
    
}