#include <stdio.h>

int main()
{
    int arr[] = {10, 20, 30, 40, 50};
    int len=sizeof(arr)/sizeof(arr[0]);
    for(int i=0;i<len;i++)
    {
        printf("%d\t",arr[i]);
    }
    printf("\n");
    for(int i=0;i<len;i++)
    {
        printf("%d\t",*(arr+i));
    }
    
    
}