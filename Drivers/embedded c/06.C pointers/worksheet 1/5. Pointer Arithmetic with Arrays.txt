#include <stdio.h>
void fun(int *a,int len)
{
    for(int i=0;i<len;i++)
    {
        printf("%d\t",*(a+i));
    }
}
int main()
{
    int arr[] = {10, 20, 30, 40, 50};
    int len=sizeof(arr)/sizeof(arr[0]);
    fun(arr,len);
}