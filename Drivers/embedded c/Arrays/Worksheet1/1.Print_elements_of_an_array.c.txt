#include <stdio.h>

int main()
{
    int num,i;
    printf("Enter the number of elements:");
    scanf("%d",&num);
    int arr[num];
    printf("Enter the elements: ");
    for(i=0;i<num;i++)
    {
        scanf("%d",&arr[i]);
    }
    printf("Elements in array are : ");
    for(i=0;i<num;i++)
    {
        printf("%d ",arr[i]);
    }
    return 0;
}