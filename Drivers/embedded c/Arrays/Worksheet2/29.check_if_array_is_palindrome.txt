#include <stdio.h>

int main()
{
    int i, j, n = 6;
    int arr1[6], arr2[6];

    printf("Input1 : ");
    for(i = 0; i < n; i++)
        scanf("%d", &arr1[i]);
        for(i=0;i<n;i++)
        {
            arr2[i]=arr1[i];
        }
    for(i=0;i<n/2;i++)
    {
        int temp=arr1[i];
        arr1[i]=arr1[n-i-1];
        arr1[n-i-1]=temp;
    }
    for(i=0;i<n;i++)
    {
        printf("%d",arr1[i]);
    }
    int flag=0;
for(i=0;i<n;i++)
{
    if(arr1[i]!=arr2[i])
    {
        flag=1;break;
    }
}
if(flag)
printf("not palindrome");
else
printf("palindrome");
    return 0;
}