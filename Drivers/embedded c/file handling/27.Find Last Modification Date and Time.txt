#include <stdio.h>
#include <sys/stat.h>
#include <time.h>

int main()
{
    struct stat fileInfo;

    if (stat("a.txt", &fileInfo) != 0)
    {
        printf("Unable to get file information\n");
        return 1;
    }

    printf("Last modified: %s", ctime(&fileInfo.st_mtime));

    return 0;
}