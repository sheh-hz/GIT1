#include <stdio.h>
#include <string.h>

int main()
{
    char a[200];
    int i, j;

    scanf("%[^\n]", a);

    int len = strlen(a);
    for(i=0;i<len;i++)
    {
        if(a[i]=='e')
        {
            printf("positions %d",i);
            break;
        }
    }
   

    return 0;
}