#include <stdio.h>
#include <string.h>

int main()
{
    char a[200];
    int i;
    scanf("%[^\n]", a);
    int len=strlen(a);
    for(i=len-1;i>0;i--)
    {
        if(a[i]=='b')
        {
            printf("%d",i);
            break;
        }
    }

    return 0;
}