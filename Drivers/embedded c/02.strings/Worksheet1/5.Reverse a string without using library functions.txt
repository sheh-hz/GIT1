#include <stdio.h>
#include <string.h>

int main()
{
    char a[100];
    int i, j, count, min, l;

    scanf("%[^\n]", a);

    int len=strlen(a);
    for(i=0;i<len/2;i++)
    {
        char temp=a[i];
        a[i]=a[len-i-1];
        a[len-i-1]=temp;
    }
    printf("%s",a);
    return 0;
}