#include <stdio.h>
#include <string.h>

void rev(char *a, int i)
{
    if (a[i] == '\0')
        return;

    rev(a, i + 1);
    printf("%c", a[i]);
}

int main()
{
    char a[] = "hello";

    rev(a, 0);

    return 0;
}