#include <stdio.h>
#include <string.h>

int main()
{
    char a[100],chara;
    int i, j, count, min, l;

    scanf(" %[^\n]", a);

    int len=strlen(a);
    printf("replace chara : ");
    scanf(" %c",&chara);
    for(i=0;i<len;i++)
    {
        if(a[i]=='a')
        {
            a[i]=chara;
        }
    }
    printf("%s",a);
    return 0;
}