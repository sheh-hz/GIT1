#include <stdio.h>
#include <string.h>

int main()
{
    char a[100],chara,word[50];
    scanf(" %[^\n]", a);
    scanf(" %[^\n]", word);

    char* pos = strstr(a, word);

    if (pos != NULL)
    {
        printf("Word found at position %ld", pos - a+1);
    }
    else
    {
        printf("Word not found");
    }

    return 0;
}