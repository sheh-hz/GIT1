#include <stdio.h>
#include <string.h>

int main()
{
    char a[100];
    int i, j, count, min, l;

    scanf("%[^\n]", a);

    int len=strlen(a);
    for(i=len-1;i>0;i++)
    {
        if(a[i]==' ')
        {
            len--;
        }
        else
            break;
    }
     a[len] = '\0';
    for(i=0;i<len;i++)
    {
        if(a[0]==' ')
        {
            for(j=i;j<len;j++)
            {
                a[j]=a[j+1];
            }
            len--;
            i--;
        }
        if(a[i]==' '&&a[i+1]==' ')
        {
            for(j=i;j<len;j++)
            {
                a[j]=a[j+1];
            }
            len--;
            i--;
        }
    }
    printf("%s",a);
    return 0;
}