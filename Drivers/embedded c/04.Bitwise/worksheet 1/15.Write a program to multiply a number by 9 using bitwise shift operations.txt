#include <stdio.h>

int multiplyBy9(int num)
{
    return (num << 3) + num;
}

int main()
{
    int num;

    printf("Enter a number: ");
    scanf("%d", &num);

    printf("Result = %d\n", multiplyBy9(num));

    return 0;
}