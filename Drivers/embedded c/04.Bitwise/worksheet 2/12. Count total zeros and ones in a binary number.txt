#include <stdio.h>

int main() {
    int n,one=0,zero=0,i=0;
    scanf("%d",&n);
    while(i<8)
    {
        if((n&1)==1)
            one++;
        else
            zero++;
            
        n=n>>1;
        i++;
    }
    printf("one : %d\nzero : %d",one,zero);

    return 0;
}