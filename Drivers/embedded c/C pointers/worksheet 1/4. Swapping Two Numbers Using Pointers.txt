#include <stdio.h>
void swap(int *a,int *b)
{
    *a=*a+*b;
    *b=*a-*b;
    *a=*a-*b;
    
    printf("after swap : %d %d",*a,*b);
}
int main()
{
   int a=10;
   int b=20;
   printf("Before swap : %d %d",a,b);

   swap(&a,&b);
    return 0;
}